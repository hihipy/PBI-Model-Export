# pbi-model-export

A Tabular Editor 2 C# script that exports a complete Power BI model to a structured JSON file optimized for AI consumption — includes tables, columns, measures with full recursive DAX dependency chains, relationships with fact/dimension role hints, hierarchies, partitions, RLS roles, and a compact summary section with top-level KPI detection, display folder grouping, and auto-generated table flagging. Output file is named automatically from the Power BI Desktop window title.

## Why

Power BI models contain structure that's invisible outside of Power BI Desktop. This script extracts everything into a single portable JSON file so you can:

- Give an AI assistant (Claude, Copilot, etc.) full context on your model without describing it manually
- Document model architecture for handoffs and reviews
- Run impact analysis on DAX measure dependencies
- Audit relationships, RLS roles, and data sources
- Compare models over time by diffing exports

## Requirements

- **Tabular Editor 2** (tested on 2.27; should work on any 2.x with C# scripting)
- **Power BI Desktop** model open in Tabular Editor (or SSAS/AAS connection)

## Installation

1. Download `PBIModelExport.csx`
2. Open your Power BI model in Tabular Editor 2
3. Go to the **Advanced Scripting** tab
4. **File → Open Script** → select the `.csx` file
5. Click **Run** (▶)
6. Find the export in your Downloads folder — named automatically after your report

**Optional — save as a Custom Action:**

- **File → Preferences → Custom Actions**
- Click **Add**, paste the script contents
- Name it "Export Full Model to JSON"
- Now available via right-click context menu on any model

## Configuration

All options are at the top of the script:
```csharp
var dashboardName = "...";      // Auto-detected from Power BI Desktop window title
var outputFolder = Path.Combine(                // Defaults to Downloads
    Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), "Downloads");
var includeHiddenObjects = true;                // Include hidden tables/columns/measures
var includePartitionSources = true;             // Include M queries and DAX partition expressions
var includeRoles = true;                        // Include RLS role definitions
```

The output file name is generated automatically from the report name — no manual naming needed. If you have `FY27 Faculty Recruitment.pbix` open, the file saves as `FY27 Faculty Recruitment_ModelExport.json`.

## What Gets Exported

| Section | Contents |
|---------|----------|
| **Model Metadata** | Compatibility level, culture, default mode (Import/DirectQuery), object counts |
| **Tables** | Name, description, visibility, auto-generated flag, source file path, data category, table type (Regular/CalculatedTable), partition source types |
| **Columns** | Name, data type, column type (Data/Calculated/CalculatedTableColumn), key status, format string, display folder, data category, summarize-by, sort-by-column |
| **Measures** | Name, full DAX expression, description, data type, format string, display folder, visibility, top-level KPI flag, direct measure dependencies, full recursive dependency chain |
| **Partitions** | Name, source type (M/Calculated/Entity), full M query or DAX expression, storage mode |
| **Hierarchies** | Name, levels with ordinal positions and source columns |
| **Relationships** | From/to table and column, cardinality, active/inactive, cross-filter direction, security filtering, fact/dimension role hints |
| **Roles (RLS)** | Role name, description, permission level, DAX filter expressions per table |
| **Data Sources** | Name, description, type |
| **Summary** | User-facing vs auto-generated table split, top-level KPI list, measures grouped by display folder, relationship map, column inventory per table, measure counts per table |

## Output Format
```json
{
  "exportDate": "2026-04-03 15:32:55",
  "dashboardName": "RDD_Dashboard",
  "modelName": "35aef230-b3dd-457e-9740-e70cda52e355",
  "scriptVersion": "3.0",
  "modelMetadata": {
    "compatibilityLevel": 1600,
    "culture": "en-US",
    "defaultMode": "Import",
    "description": "",
    "tableCount": 5,
    "relationshipCount": 1,
    "measureCount": 94,
    "roleCount": 1
  },
  "exportConfiguration": {
    "includeHiddenObjects": true,
    "includePartitionSources": true,
    "includeRoles": true
  },
  "tables": [
    {
      "name": "Sales",
      "description": "Main fact table sourced from the sales CSV export",
      "isHidden": false,
      "isAutoGenerated": false,
      "dataCategory": "",
      "sourceFilePath": "C:\\Users\\you\\OneDrive\\Data\\Sales.csv",
      "tableType": "Regular",
      "partitionSourceTypes": ["M"],
      "partitions": [
        {
          "name": "Sales",
          "sourceType": "M",
          "expression": "let\n    Source = Csv.Document(File.Contents(\"C:\\\\Users\\\\you\\\\OneDrive\\\\Data\\\\Sales.csv\"),[Delimiter=\",\", Encoding=1252]),\n    #\"Promoted Headers\" = Table.PromoteHeaders(Source, [PromoteAllScalars=true])\nin\n    #\"Promoted Headers\"",
          "mode": "Import"
        }
      ],
      "columns": [
        {
          "name": "ProductID",
          "dataType": "Int64",
          "description": "Foreign key to the Products dimension table",
          "columnType": "Data",
          "isHidden": false,
          "isKey": true,
          "formatString": "0",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "None",
          "sortByColumn": ""
        },
        {
          "name": "Amount",
          "dataType": "Double",
          "description": "",
          "columnType": "Data",
          "isHidden": false,
          "isKey": false,
          "formatString": "$#,0.00",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "Sum",
          "sortByColumn": ""
        },
        {
          "name": "SaleDate",
          "dataType": "DateTime",
          "description": "",
          "columnType": "Data",
          "isHidden": false,
          "isKey": false,
          "formatString": "mm/dd/yyyy",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "None",
          "sortByColumn": ""
        },
        {
          "name": "YearMonth",
          "dataType": "String",
          "description": "Calculated column combining year and month for time slicing",
          "columnType": "Calculated",
          "isHidden": false,
          "isKey": false,
          "formatString": "",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "None",
          "sortByColumn": ""
        }
      ],
      "measures": [
        {
          "name": "Total_Revenue",
          "expression": "SUM(Sales[Amount])",
          "description": "Sum of all sales amounts in the current filter context",
          "dataType": "Double",
          "formatString": "$#,0.00",
          "displayFolder": "01 - Base Metrics",
          "isHidden": false,
          "isTopLevelKPI": false,
          "referencedMeasures": [],
          "fullDependencyChain": []
        },
        {
          "name": "Target_Revenue",
          "expression": "SUM(Targets[Amount])",
          "description": "",
          "dataType": "Double",
          "formatString": "$#,0.00",
          "displayFolder": "01 - Base Metrics",
          "isHidden": false,
          "isTopLevelKPI": false,
          "referencedMeasures": [],
          "fullDependencyChain": []
        },
        {
          "name": "Revenue_vs_Target",
          "expression": "DIVIDE([Total_Revenue], [Target_Revenue])",
          "description": "Ratio of actual revenue to target revenue",
          "dataType": "Double",
          "formatString": "0.0%",
          "displayFolder": "02 - KPIs",
          "isHidden": false,
          "isTopLevelKPI": true,
          "referencedMeasures": ["Target_Revenue", "Total_Revenue"],
          "fullDependencyChain": ["Target_Revenue", "Total_Revenue"]
        },
        {
          "name": "3yr_Avg_Revenue",
          "expression": "AVERAGEX(VALUES(Sales[Fiscal_Year]), [Total_Revenue])",
          "description": "Three-year average of total revenue across fiscal years",
          "dataType": "Double",
          "formatString": "$#,0.00",
          "displayFolder": "03 - Trend Analysis",
          "isHidden": false,
          "isTopLevelKPI": false,
          "referencedMeasures": ["Total_Revenue"],
          "fullDependencyChain": ["Total_Revenue"]
        },
        {
          "name": "Revenue_Growth_vs_3yr_Avg",
          "expression": "DIVIDE([Total_Revenue] - [3yr_Avg_Revenue], [3yr_Avg_Revenue])",
          "description": "Percentage growth of current revenue over the 3-year average",
          "dataType": "Double",
          "formatString": "0.0%",
          "displayFolder": "03 - Trend Analysis",
          "isHidden": false,
          "isTopLevelKPI": true,
          "referencedMeasures": ["3yr_Avg_Revenue", "Total_Revenue"],
          "fullDependencyChain": ["3yr_Avg_Revenue", "Total_Revenue"]
        }
      ],
      "hierarchies": [
        {
          "name": "Sales Date Hierarchy",
          "description": "",
          "isHidden": false,
          "levels": [
            {
              "name": "Year",
              "ordinal": 0,
              "column": "Year"
            },
            {
              "name": "Quarter",
              "ordinal": 1,
              "column": "Quarter"
            },
            {
              "name": "Month",
              "ordinal": 2,
              "column": "Month"
            }
          ]
        }
      ]
    },
    {
      "name": "Products",
      "description": "Product dimension table",
      "isHidden": false,
      "isAutoGenerated": false,
      "dataCategory": "",
      "sourceFilePath": "",
      "tableType": "Regular",
      "partitionSourceTypes": ["M"],
      "partitions": [
        {
          "name": "Products",
          "sourceType": "M",
          "expression": "let\n    Source = Sql.Database(\"server\", \"database\"),\n    dbo_Products = Source{[Schema=\"dbo\",Item=\"Products\"]}[Data]\nin\n    dbo_Products",
          "mode": "Import"
        }
      ],
      "columns": [
        {
          "name": "ProductID",
          "dataType": "Int64",
          "description": "Primary key",
          "columnType": "Data",
          "isHidden": false,
          "isKey": true,
          "formatString": "0",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "None",
          "sortByColumn": ""
        },
        {
          "name": "ProductName",
          "dataType": "String",
          "description": "",
          "columnType": "Data",
          "isHidden": false,
          "isKey": false,
          "formatString": "",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "None",
          "sortByColumn": ""
        },
        {
          "name": "Category",
          "dataType": "String",
          "description": "",
          "columnType": "Data",
          "isHidden": false,
          "isKey": false,
          "formatString": "",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "None",
          "sortByColumn": ""
        }
      ],
      "measures": [],
      "hierarchies": [
        {
          "name": "Product Hierarchy",
          "description": "",
          "isHidden": false,
          "levels": [
            {
              "name": "Category",
              "ordinal": 0,
              "column": "Category"
            },
            {
              "name": "ProductName",
              "ordinal": 1,
              "column": "ProductName"
            }
          ]
        }
      ]
    },
    {
      "name": "FY_Selector",
      "description": "Disconnected slicer table for fiscal year selection via DAX SELECTEDVALUE pattern",
      "isHidden": false,
      "isAutoGenerated": false,
      "dataCategory": "",
      "sourceFilePath": "",
      "tableType": "CalculatedTable",
      "partitionSourceTypes": ["Calculated"],
      "partitions": [
        {
          "name": "FY_Selector",
          "sourceType": "Calculated",
          "expression": "DATATABLE(\"Selected_FY\", STRING, {{\"FY2024\"},{\"FY2025\"},{\"FY2026\"}})",
          "mode": "Import"
        }
      ],
      "columns": [
        {
          "name": "Selected_FY",
          "dataType": "String",
          "description": "",
          "columnType": "CalculatedTableColumn",
          "isHidden": false,
          "isKey": false,
          "formatString": "",
          "displayFolder": "",
          "dataCategory": "",
          "summarizeBy": "None",
          "sortByColumn": ""
        }
      ],
      "measures": [],
      "hierarchies": []
    },
    {
      "name": "DateTableTemplate_83bf7f2b-3c01-4232-a208-bba871bee38a",
      "description": "",
      "isHidden": true,
      "isAutoGenerated": true,
      "dataCategory": "",
      "sourceFilePath": "",
      "tableType": "CalculatedTable",
      "partitionSourceTypes": ["Calculated"],
      "partitions": [
        {
          "name": "DateTableTemplate_83bf7f2b-3c01-4232-a208-bba871bee38a",
          "sourceType": "Calculated",
          "expression": "Calendar(Date(2015,1,1), Date(2015,1,1))",
          "mode": "Import"
        }
      ],
      "columns": [
        {
          "name": "Date",
          "dataType": "DateTime",
          "description": "",
          "columnType": "CalculatedTableColumn",
          "isHidden": true,
          "isKey": false,
          "formatString": "",
          "displayFolder": "",
          "dataCategory": "PaddedDateTableDates",
          "summarizeBy": "None",
          "sortByColumn": ""
        }
      ],
      "measures": [],
      "hierarchies": []
    }
  ],
  "relationships": [
    {
      "name": "'Sales'[ProductID] --> 'Products'[ProductID]",
      "isActive": true,
      "fromTable": "Sales",
      "fromColumn": "ProductID",
      "fromCardinality": "Many",
      "toTable": "Products",
      "toColumn": "ProductID",
      "toCardinality": "One",
      "crossFilteringBehavior": "OneDirection",
      "securityFilteringBehavior": "OneDirection",
      "factTable": "Sales",
      "dimensionTable": "Products"
    }
  ],
  "roles": [
    {
      "name": "Sales_Region_Filter",
      "description": "Restricts data to the user's assigned region",
      "modelPermission": "Read",
      "tablePermissions": [
        {
          "table": "Sales",
          "filterExpression": "[Region] = USERPRINCIPALNAME()"
        }
      ]
    }
  ],
  "dataSources": [
    {
      "name": "SQL_Warehouse",
      "description": "Production SQL Server data warehouse",
      "type": "Provider"
    }
  ],
  "summary": {
    "userFacingTables": ["Sales", "Products", "FY_Selector"],
    "autoGeneratedTables": ["DateTableTemplate_83bf7f2b-3c01-4232-a208-bba871bee38a"],
    "topLevelKPIs": ["Revenue_Growth_vs_3yr_Avg", "Revenue_vs_Target"],
    "measuresByFolder": {
      "01 - Base Metrics": ["Total_Revenue", "Target_Revenue"],
      "02 - KPIs": ["Revenue_vs_Target"],
      "03 - Trend Analysis": ["3yr_Avg_Revenue", "Revenue_Growth_vs_3yr_Avg"]
    },
    "relationshipMap": [
      "Sales.ProductID --> Products.ProductID (Many:One)"
    ],
    "columnInventory": {
      "Sales": ["ProductID (Int64)", "Amount (Double)", "SaleDate (DateTime)", "YearMonth (String)"],
      "Products": ["ProductID (Int64)", "ProductName (String)", "Category (String)"],
      "FY_Selector": ["Selected_FY (String)"],
      "DateTableTemplate_83bf7f2b-3c01-4232-a208-bba871bee38a": ["Date (DateTime)"]
    },
    "measuresPerTable": {
      "Sales": 5
    }
  }
}
```

The `summary` section is intentionally compact — it gives an AI the full model shape in a few lines without parsing nested objects. `userFacingTables` and `autoGeneratedTables` let AI immediately deprioritize Power BI internal tables. `topLevelKPIs` surfaces the measures that are not referenced by any other measure and are therefore your user-facing outputs. `measuresByFolder` maps the full organizational structure of your measure tree so AI can navigate directly to the relevant group without scanning every measure.

## Technical Details

### Automatic File Naming

The script reads the Power BI Desktop process window title at runtime (`"ReportName - Power BI Desktop"`) and parses out the report name. The output file is saved as `ReportName_ModelExport.json` with any characters invalid for Windows filenames stripped automatically. No manual configuration needed when switching between reports.

### JSON Escaping

The script uses a character-level escaper that handles all JSON-unsafe characters: `\"`, `\\`, `\b`, `\f`, `\n`, `\r`, `\t`, and any control character below `U+0020` (emitted as `\uXXXX`). This prevents malformed JSON from DAX expressions, M queries, or descriptions containing special characters.

### Measure Dependency Detection

Direct dependencies are extracted using regex bracket-token matching (`\[([^\]]+)\]`) against a pre-built `HashSet` of all measure names. Full recursive dependency chains are resolved by walking the dependency graph with circular reference protection, so `fullDependencyChain` on any measure reflects every upstream measure it relies on at any depth.

### Top-Level KPI Detection

A reverse lookup is built across all measure expressions before export. Any measure whose name never appears as a dependency in another measure's expression is flagged `isTopLevelKPI: true` and included in the `topLevelKPIs` summary list. These are your model's entry points.

### Auto-Generated Table Detection

Tables whose names contain a GUID pattern (e.g. `DateTableTemplate_83bf7f2b-3c01-4232-...`) are flagged `isAutoGenerated: true`. These are Power BI internal tables created automatically for date intelligence and are surfaced separately in the summary so AI can deprioritize them.

### Source File Path Extraction

For tables loaded from flat files, the script parses the `File.Contents("...")` call out of the M partition expression using regex and surfaces it as a clean `sourceFilePath` field at the table level — no need to dig through raw M code.

### Relationship Role Hints

Each relationship includes `factTable` and `dimensionTable` fields derived from cardinality: the `Many` side is the fact table, the `One` side is the dimension table.

### Comma Safety

All array and object sections are built as string lists and joined with commas (`string.Join`), eliminating trailing comma bugs when adding or removing optional sections.

### Output Encoding

UTF-8 without BOM (`new UTF8Encoding(false)`) for maximum compatibility with JSON parsers, Python, and web tools.

### Table Type Detection

Table type is inferred from partition source types (a partition with source type `Calculated` → `CalculatedTable`). The raw `partitionSourceTypes` array is also exported so you can verify or refine the classification downstream.

## Integration Examples

### Feed to an AI Assistant

Upload the JSON file directly to Claude, ChatGPT, or any LLM and ask questions like:

- "Review my DAX measures for performance issues"
- "What happens if I rename the Department column?"
- "Suggest measures I'm missing for this type of dashboard"
- "Document this model for my team"
- "Which top-level KPIs depend on this base measure?"

### Python Analysis
```python
import json

with open("RDD_Dashboard_ModelExport.json", "r", encoding="utf-8") as f:
    model = json.load(f)

print(f"Dashboard: {model['dashboardName']}")
print(f"Tables: {model['modelMetadata']['tableCount']}")
print(f"Measures: {model['modelMetadata']['measureCount']}")

# User-facing tables only
print("\nUser-facing tables:")
for t in model["summary"]["userFacingTables"]:
    print(f"  {t}")

# Top-level KPIs
print("\nTop-level KPIs:")
for kpi in model["summary"]["topLevelKPIs"]:
    print(f"  {kpi}")

# Measures with deep dependency chains
print("\nComplex measures:")
for table in model["tables"]:
    for m in table["measures"]:
        chain = m["fullDependencyChain"]
        if len(chain) >= 5:
            print(f"  {m['name']} -> {len(chain)} upstream measures")

# Measures by folder
print("\nMeasure folders:")
for folder, measures in model["summary"]["measuresByFolder"].items():
    print(f"  {folder}: {len(measures)} measures")

# Fact vs dimension tables from relationships
print("\nRelationship roles:")
for r in model["relationships"]:
    print(f"  Fact: {r['factTable']} | Dimension: {r['dimensionTable']}")
```

### Power Query Import
```powerquery
let
    Source = Json.Document(File.Contents("RDD_Dashboard_ModelExport.json")),
    Tables = Source[tables],
    TableList = Table.FromList(Tables, Splitter.SplitByNothing()),
    Expanded = Table.ExpandRecordColumn(TableList, "Column1",
        {"name", "tableType", "isHidden", "isAutoGenerated", "sourceFilePath", "columns", "measures"})
in
    Expanded
```

## File Locations

The script automatically saves to your Downloads folder, named after the open report:

| OS | Path |
|----|------|
| Windows | `C:\Users\{username}\Downloads\{ReportName}_ModelExport.json` |
| Mac | `/Users/{username}/Downloads/{ReportName}_ModelExport.json` |
| Linux | `/home/{username}/Downloads/{ReportName}_ModelExport.json` |

The output folder is created automatically if it doesn't exist.

## Troubleshooting

**Script compiles but output is empty:**
Check that your model has tables. Open the Tables pane in Tabular Editor to verify the model loaded.

**Dashboard name shows as "UnknownReport":**
The script reads the Power BI Desktop window title. Make sure the report is open and fully loaded in Power BI Desktop before running the script in Tabular Editor.

**"Expression" is blank on partitions:**
Some partition types (e.g., `Entity` in DirectQuery) don't expose source expressions. The script catches this gracefully — the field will be an empty string.

**`sourceFilePath` is empty:**
Only tables loaded via `File.Contents()` in their M expression will have this populated. SQL, SharePoint, and other source types won't produce a file path.

**Hidden tables/columns missing:**
Set `includeHiddenObjects = true` in the configuration section.

**File not appearing in Downloads:**
Check the Tabular Editor output pane for the full path. The script prints the output location on success.

## Changelog

### v3.0 (Current)

- Auto-detects report name from Power BI Desktop window title — output file named automatically
- `isAutoGenerated` flag on tables containing GUIDs (Power BI internal tables)
- `sourceFilePath` field extracted from M partition expressions
- `isTopLevelKPI` flag per measure — measures not referenced by any other measure
- `fullDependencyChain` per measure — full recursive upstream dependency resolution with circular reference protection
- `factTable` / `dimensionTable` role hints on relationships derived from cardinality
- Summary expanded with `userFacingTables`, `autoGeneratedTables`, `topLevelKPIs`, and `measuresByFolder`
- Console summary now reports auto-generated table count, KPI count, and display folder count

### v2.0

- Full model export: tables, columns, relationships, partitions, hierarchies, roles, data sources
- Proper JSON escaping for all control characters
- Regex-based measure dependency detection (faster, fewer false positives)
- Comma-safe output using list-join pattern
- Table type detection with raw `partitionSourceTypes` fallback
- UTF-8 without BOM
- Output folder auto-creation
- Compact `summary` section for AI consumption

### v1.0

- Measures and calculated columns only
- Basic string-replace JSON escaping
- O(n²) dependency detection via `.Contains()`
- Manual comma tracking

## License

[CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/)

You are free to use, share, and adapt this work, including at your job, under these terms:

- **Attribution** — Credit the original author
- **NonCommercial** — No selling or commercial products
- **ShareAlike** — Derivatives must use the same license
